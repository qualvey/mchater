## fixed

[x] 前端UI可以识别文件，但是无法触发发送。管理员侧收不到接收文件的提醒。
[x] 管理员侧无法发送文件。
[x] 管理员无法登录。点击登录按钮，没有反应，devtool network没有新请求
[x] 客户端发送文件，UI会显示待管理员审核。管理员侧没有响应。服务器无相关日志

[x] 用户侧输入时，管理员侧显示用户正在输入。反之不工作
[x] 存在多个管理员，用户只能看到自动推荐

## unfixed

[x] 移动端模式下普通用户对话窗口被 CSS 全局规则平移出屏幕导致的页面空白问题
[x] 修复 join-user 回调中残留 adminSockets.size 变量引发的 ReferenceError 崩溃问题
[x] 修复 revealAdminLogin 中残留 inputAdminKey 引起的 ReferenceError 崩溃及旧 Session 鉴权无法自动恢复问题
[x] 消息管线不对，用户在自动推荐下发送的消息只有主管理收到。主管理回复给用户的消息全局多管理实时同步
[x] 前端点击团队内部按钮后，对象列表无延迟实时刷新
[x] 超级管理员点击内部频道，没有内部的对象显示。列表里显示的普通用户，给任意普通用户发消息都会在内部广播频道显示
[x] 管理员用户，点击内部频道按钮后，下面的用户列表只显示内部成员列表（修复 DOM 变量引用别名导致的列表未隐去隐患）